<!-- RatingDialog.xaml -->
<Window x:Class="MyApp.RatingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edycja oceny" SizeToContent="WidthAndHeight" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Wybierz ocenę (1-10):" Margin="0,0,0,10" Grid.Row="0"/>

        <Slider x:Name="RatingSlider" Minimum="1" Maximum="10" 
                Value="5" IsSnapToTickEnabled="True" 
                TickFrequency="1" Grid.Row="1" Margin="0,0,0,10"/>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <TextBlock x:Name="RatingValueText" Text="5" VerticalAlignment="Center" Margin="0,0,15,0" FontWeight="Bold"/>
            <Button Content="OK" IsDefault="True" Click="OkButton_Click" Width="75" Margin="0,0,5,0"/>
            <Button Content="Anuluj" IsCancel="True" Width="75"/>
        </StackPanel>
    </Grid>
</Window>